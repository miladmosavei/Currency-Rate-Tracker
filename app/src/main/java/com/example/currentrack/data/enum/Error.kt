package com.example.currentrack.data.enum

/**
 * To handle the type of error that will be returned by the repository
 *
 */
enum class Error {
    NullObject,
    VALIDATION_ERROR,
    NETWORK_ERROR
}